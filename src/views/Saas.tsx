"use client";

import { MarketingPageLayout } from "@/components/landing";
import { saasContent } from "@/content/marketing-pages";

const Saas = () => {
  return <MarketingPageLayout content={saasContent} />;
};

export default Saas;
