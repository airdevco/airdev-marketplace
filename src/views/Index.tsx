"use client";

import { MarketingPageLayout } from "@/components/landing";
import { indexContent } from "@/content/marketing-pages";

const Index = () => {
  return <MarketingPageLayout content={indexContent} />;
};

export default Index;
